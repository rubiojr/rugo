# Auto-generated smoke tests by rugo dev bridgegen; DO NOT EDIT.
use "test"
import "strings"

rats "strings.clone is callable"
  result = try strings.clone("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.compare is callable"
  result = try strings.compare("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "strings.contains is callable"
  result = try strings.contains("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "strings.contains_any is callable"
  result = try strings.contains_any("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "strings.count is callable"
  result = try strings.count("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "strings.cut is callable"
  result = try strings.cut("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "strings.equal_fold is callable"
  result = try strings.equal_fold("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "strings.fields is callable"
  result = try strings.fields("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "strings.has_prefix is callable"
  result = try strings.has_prefix("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "strings.has_suffix is callable"
  result = try strings.has_suffix("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "strings.index is callable"
  result = try strings.index("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "strings.index_any is callable"
  result = try strings.index_any("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "strings.join is callable"
  result = try strings.join(["a"], "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.last_index is callable"
  result = try strings.last_index("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "strings.last_index_any is callable"
  result = try strings.last_index_any("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "strings.repeat is callable"
  result = try strings.repeat("a", 1) or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.replace is callable"
  result = try strings.replace("a", "a", "a", 1) or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.replace_all is callable"
  result = try strings.replace_all("a", "a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.split is callable"
  result = try strings.split("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "strings.split_after is callable"
  result = try strings.split_after("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "strings.split_after_n is callable"
  result = try strings.split_after_n("a", "a", 1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "strings.split_n is callable"
  result = try strings.split_n("a", "a", 1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "strings.title is callable"
  result = try strings.title("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.to_lower is callable"
  result = try strings.to_lower("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.to_title is callable"
  result = try strings.to_title("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.to_upper is callable"
  result = try strings.to_upper("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.to_valid_utf_8 is callable"
  result = try strings.to_valid_utf_8("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.trim is callable"
  result = try strings.trim("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.trim_left is callable"
  result = try strings.trim_left("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.trim_prefix is callable"
  result = try strings.trim_prefix("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.trim_right is callable"
  result = try strings.trim_right("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.trim_space is callable"
  result = try strings.trim_space("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.trim_suffix is callable"
  result = try strings.trim_suffix("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "strings.contains_rune is callable"
  result = try strings.contains_rune("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "strings.index_byte is callable"
  result = try strings.index_byte("a", 0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "strings.index_rune is callable"
  result = try strings.index_rune("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "strings.last_index_byte is callable"
  result = try strings.last_index_byte("a", 0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

