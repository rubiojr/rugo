# Functions

Define functions with `def` and return values with `return`.

```ruby
def add(a, b)
  return a + b
end

puts add(2, 3)
```

```text
5
```

No-arg functions can omit parentheses:

```ruby
def say_hello
  puts "hello"
end

say_hello
```

```text
hello
```
