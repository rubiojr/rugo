# String interpolation in Rugo

name = "World"
age = 25
puts("Hello, #{name}! You are #{age} years old.")

# Interpolation with expressions
x = 10
y = 20
sum = x + y
puts("#{x} + #{y} = #{sum}")

# Nested string usage
greeting = "Hi #{name}"
puts(greeting)
