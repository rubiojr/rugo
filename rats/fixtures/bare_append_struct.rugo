struct Item
  name
end

def build_items(names)
  items = []
  for n in names
    append items, Item(n)
  end
  return items
end

result = build_items(["apple", "banana", "cherry"])
puts result[0]["name"]
puts result[1]["name"]
puts result[2]["name"]
