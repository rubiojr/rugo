# Control Flow

Use `if`, `elsif`, and `else` for branching.

```ruby
score = 85

if score >= 90
  puts "A"
elsif score >= 80
  puts "B"
else
  puts "C"
end
```

```text
B
```

Boolean operators: `&&`, `||`, `!`.
