# Nested closure mutation: inner lambda mutates outer scope var
def wrapper()
  val = 0
  outer = fn()
    inner = fn()
      val = val + 1
    end
    inner()
  end
  outer()
  return val
end

puts wrapper()
