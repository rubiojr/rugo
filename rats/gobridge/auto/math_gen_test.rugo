# Auto-generated smoke tests by rugo dev bridgegen; DO NOT EDIT.
use "test"
import "math"

rats "math.abs is callable"
  result = try math.abs(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.acos is callable"
  result = try math.acos(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.acosh is callable"
  result = try math.acosh(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.asin is callable"
  result = try math.asin(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.asinh is callable"
  result = try math.asinh(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.atan is callable"
  result = try math.atan(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.atan2 is callable"
  result = try math.atan2(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.atanh is callable"
  result = try math.atanh(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.cbrt is callable"
  result = try math.cbrt(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.ceil is callable"
  result = try math.ceil(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.copysign is callable"
  result = try math.copysign(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.cos is callable"
  result = try math.cos(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.cosh is callable"
  result = try math.cosh(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.dim is callable"
  result = try math.dim(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.erf is callable"
  result = try math.erf(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.erfc is callable"
  result = try math.erfc(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.erfcinv is callable"
  result = try math.erfcinv(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.erfinv is callable"
  result = try math.erfinv(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.exp is callable"
  result = try math.exp(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.exp2 is callable"
  result = try math.exp2(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.expm1 is callable"
  result = try math.expm1(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.floor is callable"
  result = try math.floor(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.fma is callable"
  result = try math.fma(1.0, 1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.frexp is callable"
  result = try math.frexp(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "math.gamma is callable"
  result = try math.gamma(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.hypot is callable"
  result = try math.hypot(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.ilogb is callable"
  result = try math.ilogb(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "math.inf is callable"
  result = try math.inf(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.is_inf is callable"
  result = try math.is_inf(1.0, 1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "math.is_nan is callable"
  result = try math.is_nan(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "math.j0 is callable"
  result = try math.j0(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.j1 is callable"
  result = try math.j1(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.jn is callable"
  result = try math.jn(1, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.ldexp is callable"
  result = try math.ldexp(1.0, 1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.lgamma is callable"
  result = try math.lgamma(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "math.log is callable"
  result = try math.log(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.log10 is callable"
  result = try math.log10(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.log1p is callable"
  result = try math.log1p(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.log2 is callable"
  result = try math.log2(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.logb is callable"
  result = try math.logb(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.max is callable"
  result = try math.max(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.min is callable"
  result = try math.min(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.mod is callable"
  result = try math.mod(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.modf is callable"
  result = try math.modf(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "math.nan is callable"
  result = try math.nan() or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.nextafter is callable"
  result = try math.nextafter(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.pow is callable"
  result = try math.pow(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.pow10 is callable"
  result = try math.pow10(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.remainder is callable"
  result = try math.remainder(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.round is callable"
  result = try math.round(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.round_to_even is callable"
  result = try math.round_to_even(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.signbit is callable"
  result = try math.signbit(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "math.sin is callable"
  result = try math.sin(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.sincos is callable"
  result = try math.sincos(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

rats "math.sinh is callable"
  result = try math.sinh(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.sqrt is callable"
  result = try math.sqrt(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.tan is callable"
  result = try math.tan(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.tanh is callable"
  result = try math.tanh(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.trunc is callable"
  result = try math.trunc(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.y0 is callable"
  result = try math.y0(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.y1 is callable"
  result = try math.y1(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.yn is callable"
  result = try math.yn(1, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.float32bits is callable"
  result = try math.float32bits(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "math.float32frombits is callable"
  result = try math.float32frombits(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.float64bits is callable"
  result = try math.float64bits(1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "math.float64frombits is callable"
  result = try math.float64frombits(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

rats "math.nextafter32 is callable"
  result = try math.nextafter32(1.0, 1.0) or nil
  if result != nil
    test.assert_eq(type_of(result), "Float")
  end
end

