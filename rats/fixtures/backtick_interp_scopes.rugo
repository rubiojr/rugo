def greet(name)
  return `echo hello #{name}`
end
puts greet("rugo")

prefix = "hi"
greeter = fn(name)
  return `echo #{prefix} #{name}`
end
puts greeter("world")

names = ["alice", "bob"]
for name in names
  puts `echo hey #{name}`
end

flag = true
if flag
  puts `echo flag #{flag}`
end
