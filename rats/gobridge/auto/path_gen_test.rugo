# Auto-generated smoke tests by rugo dev bridgegen; DO NOT EDIT.
use "test"
import "path"

rats "path.base is callable"
  result = try path.base("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "path.clean is callable"
  result = try path.clean("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "path.dir is callable"
  result = try path.dir("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "path.ext is callable"
  result = try path.ext("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "path.is_abs is callable"
  result = try path.is_abs("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "path.join is callable"
  result = try path.join(["a"]) or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "path.match is callable"
  result = try path.match("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "path.split is callable"
  result = try path.split("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Array")
  end
end

