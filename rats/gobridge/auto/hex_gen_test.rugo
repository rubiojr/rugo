# Auto-generated smoke tests by rugo dev bridgegen; DO NOT EDIT.
use "test"
import "encoding/hex"

rats "hex.decoded_len is callable"
  result = try hex.decoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "hex.encoded_len is callable"
  result = try hex.encoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "hex.append_decode is callable"
  result = try hex.append_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "hex.append_encode is callable"
  result = try hex.append_encode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "hex.decode_string is callable"
  result = try hex.decode_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "hex.dump is callable"
  result = try hex.dump("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "hex.encode_to_string is callable"
  result = try hex.encode_to_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

