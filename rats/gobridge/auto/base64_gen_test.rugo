# Auto-generated smoke tests by rugo dev bridgegen; DO NOT EDIT.
use "test"
import "encoding/base64"

rats "base64.raw_std_encoding_decoded_len is callable"
  result = try base64.raw_std_encoding_decoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.raw_std_encoding_encoded_len is callable"
  result = try base64.raw_std_encoding_encoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.raw_url_encoding_decoded_len is callable"
  result = try base64.raw_url_encoding_decoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.raw_url_encoding_encoded_len is callable"
  result = try base64.raw_url_encoding_encoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.std_encoding_decoded_len is callable"
  result = try base64.std_encoding_decoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.std_encoding_encoded_len is callable"
  result = try base64.std_encoding_encoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.url_encoding_decoded_len is callable"
  result = try base64.url_encoding_decoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.url_encoding_encoded_len is callable"
  result = try base64.url_encoding_encoded_len(1) or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.raw_std_encoding_append_decode is callable"
  result = try base64.raw_std_encoding_append_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.raw_std_encoding_append_encode is callable"
  result = try base64.raw_std_encoding_append_encode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.raw_std_encoding_decode is callable"
  result = try base64.raw_std_encoding_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.raw_std_encoding_decode_string is callable"
  result = try base64.raw_std_encoding_decode_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.raw_std_encoding_encode_to_string is callable"
  result = try base64.raw_std_encoding_encode_to_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.raw_url_encoding_append_decode is callable"
  result = try base64.raw_url_encoding_append_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.raw_url_encoding_append_encode is callable"
  result = try base64.raw_url_encoding_append_encode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.raw_url_encoding_decode is callable"
  result = try base64.raw_url_encoding_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.raw_url_encoding_decode_string is callable"
  result = try base64.raw_url_encoding_decode_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.raw_url_encoding_encode_to_string is callable"
  result = try base64.raw_url_encoding_encode_to_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.std_encoding_append_decode is callable"
  result = try base64.std_encoding_append_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.std_encoding_append_encode is callable"
  result = try base64.std_encoding_append_encode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.std_encoding_decode is callable"
  result = try base64.std_encoding_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.std_encoding_decode_string is callable"
  result = try base64.std_encoding_decode_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.std_encoding_encode_to_string is callable"
  result = try base64.std_encoding_encode_to_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.url_encoding_append_decode is callable"
  result = try base64.url_encoding_append_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.url_encoding_append_encode is callable"
  result = try base64.url_encoding_append_encode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.url_encoding_decode is callable"
  result = try base64.url_encoding_decode("a", "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Integer")
  end
end

rats "base64.url_encoding_decode_string is callable"
  result = try base64.url_encoding_decode_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "base64.url_encoding_encode_to_string is callable"
  result = try base64.url_encoding_encode_to_string("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

