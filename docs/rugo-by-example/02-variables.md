# Variables

Rugo variables are dynamically typed and need no declarations.

```ruby
name = "Rugo"
age = 1
score = 9.5
active = true
empty = nil

puts "#{name} v#{age}"
```

```text
Rugo v1
```

You can freely reassign values:

```ruby
x = 10
x = "now a string"
puts x
```

```text
now a string
```
