# Auto-generated smoke tests by rugo dev bridgegen; DO NOT EDIT.
use "test"
import "unicode"

rats "unicode.azeri_case_to_lower is callable"
  result = try unicode.azeri_case_to_lower("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.azeri_case_to_title is callable"
  result = try unicode.azeri_case_to_title("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.azeri_case_to_upper is callable"
  result = try unicode.azeri_case_to_upper("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.is_control is callable"
  result = try unicode.is_control("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_digit is callable"
  result = try unicode.is_digit("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_graphic is callable"
  result = try unicode.is_graphic("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_letter is callable"
  result = try unicode.is_letter("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_lower is callable"
  result = try unicode.is_lower("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_mark is callable"
  result = try unicode.is_mark("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_number is callable"
  result = try unicode.is_number("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_print is callable"
  result = try unicode.is_print("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_punct is callable"
  result = try unicode.is_punct("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_space is callable"
  result = try unicode.is_space("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_symbol is callable"
  result = try unicode.is_symbol("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_title is callable"
  result = try unicode.is_title("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.is_upper is callable"
  result = try unicode.is_upper("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "Bool")
  end
end

rats "unicode.simple_fold is callable"
  result = try unicode.simple_fold("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.to is callable"
  result = try unicode.to(1, "a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.to_lower is callable"
  result = try unicode.to_lower("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.to_title is callable"
  result = try unicode.to_title("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.to_upper is callable"
  result = try unicode.to_upper("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.turkish_case_to_lower is callable"
  result = try unicode.turkish_case_to_lower("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.turkish_case_to_title is callable"
  result = try unicode.turkish_case_to_title("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

rats "unicode.turkish_case_to_upper is callable"
  result = try unicode.turkish_case_to_upper("a") or nil
  if result != nil
    test.assert_eq(type_of(result), "String")
  end
end

