def build_matrix(rows, cols)
  matrix = []
  for r in rows
    row = []
    for c in cols
      append row, r * c
    end
    append matrix, row
  end
  return matrix
end

m = build_matrix([1, 2, 3], [10, 20])
puts m[0][0]
puts m[0][1]
puts m[1][0]
puts m[1][1]
puts m[2][0]
puts m[2][1]
